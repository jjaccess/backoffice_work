// src/environments/environment.prod.ts  (producción)
export const environment = {
  production: true,
  apiUrl: 'http://apps.redorinoquiaycaribe.co:3000/api'  // ← Cambiar por IP del servidor en red local
};
